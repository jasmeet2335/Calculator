The defer attribute is used to indicate that a script is executes after the document has been parsed. 

We used Data attributes as well as normal HTML classes to make sure that we are aware which part of the HTML file is used by what.
In short, HTML classes were used by CSS and Data Attributes were used for JavaScript. 
